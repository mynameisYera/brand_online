class SimpleTaskIndex {
  String title;
  int index;
  bool isCompleted;
  bool isCashback;

  SimpleTaskIndex({
    required this.title,
    required this.index,
    required this.isCompleted,
    required this.isCashback,
});
}